Ideas:
- Make a UI/terminal app for data cleaning (netID_extractor) and web automation to retrieve user data (use ldap API)

- Expand the app to include analyses (At least start off by processing data to produce tables to make analyses easier)